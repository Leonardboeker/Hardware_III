---
phase: 02.1-height-slider
plan: 04
type: execute
wave: 3
depends_on:
  - 02.1-01
  - 02.1-02
  - 02.1-03
files_modified:
  - touchdesigner/VERTICAL-SLICE-RUNBOOK.md
  - touchdesigner/TD-FRAMEWORK-GUIDE.md
autonomous: false
requirements:
  - INP-03
must_haves:
  truths:
    - "VERTICAL-SLICE-RUNBOOK.md has a Height Slider section with smoke-test steps (slider sweep, jitter test, floor-cap test)"
    - "TD-FRAMEWORK-GUIDE.md mentions the new Serial DAT outputs (floor, slider_raw, slider_alive)"
    - "End-to-end demo proven: slider movement -> `FLOOR:N` printed in TD textport -> `compute_state['floor'][0]` updates within 100 ms -> stats overlay shows new floor count"
    - "Jitter test passes: holding slider at boundary for >=5 s does NOT oscillate floor value"
    - "Cap test passes: with 3D PRINTED selected, moving slider above floor 1 triggers visual INVALID feedback"
  artifacts:
    - path: "touchdesigner/VERTICAL-SLICE-RUNBOOK.md"
      provides: "Height slider smoke-test procedure"
      contains: "Height Slider"
    - path: "touchdesigner/TD-FRAMEWORK-GUIDE.md"
      provides: "Documented compute_state slider channels"
      contains: "floor"
  key_links:
    - from: "physical slider hardware"
      to: "TD stats overlay"
      via: "ESP32 firmware -> Serial DAT -> compute_state -> panel_text bar_bottom_status"
      pattern: "FLOOR|floor"
---

<objective>
Wrap Phase 02.1 by documenting the new pipeline in the runbook + framework guide and running an end-to-end smoke test that exercises the full slider chain: physical movement -> firmware -> Serial -> TD parsing -> compute_state -> stats overlay -> floor-cap visual feedback. This is the gate that proves the whole phase works as a system, not just as four isolated edits.

Purpose: A documented runbook turns this from a one-time integration into a reproducible demo. The smoke test is the goal-backward verification check that makes the must_haves observable.
Output: Two updated TD docs + a human-verified end-to-end test result.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-height-slider/02.1-CONTEXT.md
@.planning/phases/02.1-height-slider/02.1-01-firmware-slider-adc-PLAN.md
@.planning/phases/02.1-height-slider/02.1-02-td-serial-parser-and-compute-state-PLAN.md
@.planning/phases/02.1-height-slider/02.1-03-data-and-contracts-PLAN.md
@touchdesigner/VERTICAL-SLICE-RUNBOOK.md
@touchdesigner/TD-FRAMEWORK-GUIDE.md

<interfaces>
<!-- Phase 02.1 producer/consumer summary (for the runbook write-up): -->
ESP32 firmware (esp32_rfid.ino)
   |-- emits: BOOT, HB, RFID, FLOOR, SLIDER on USB Serial @115200
       |-- Serial DAT (rfid_in) -- onReceive parses, stores method_id/floor/slider_raw/slider_last_frame
           |-- compute_state (vision2_state_chop.py) -- fetches storage, emits 3 new channels: floor, slider_raw, slider_alive
               |-- panel_text.bar_bottom_status -- renders "FLOOR N" segment
               |-- footprint_viz_v5.cook() -- draws red border + X on method panel when floor > max_floors
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a "Height Slider" section to VERTICAL-SLICE-RUNBOOK.md with smoke-test procedure</name>
  <files>touchdesigner/VERTICAL-SLICE-RUNBOOK.md</files>
  <read_first>
    - touchdesigner/VERTICAL-SLICE-RUNBOOK.md (current runbook — Prerequisites at line 7, Hardware Setup at line 17, the Step 5 visual-state test table at lines 100-108)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (Verification path block — demo loop, jitter test, cap test)
    - .planning/phases/02.1-height-slider/02.1-01-firmware-slider-adc-PLAN.md (firmware behaviour expectations)
    - .planning/phases/02.1-height-slider/02.1-02-td-serial-parser-and-compute-state-PLAN.md (TD parsing expectations)
  </read_first>
  <action>
    Edit `touchdesigner/VERTICAL-SLICE-RUNBOOK.md`.

    1. AFTER the entire numbered list ending with the dash-bulleted `--dry-run` sub-line in the "Hardware Setup" section (currently lines 23-26), and BEFORE the section divider `---` at line 28, append a new sub-section verbatim:

       ```
       
       ### Height Slider (Phase 02.1, optional for closed-loop CV demo)
       
       The slider replaces ArUco-Dial ID-10 as the HEIGHT input. For the closed-loop CV vertical
       slice this section is optional (HEIGHT state is not exercised by the one-puck demo); for the
       full FSM smoke test it is required.
       
       5. Wire the **DollaTek 10K linear slide potentiometer**:
          - VCC -> ESP32 **3V3** (shares rail with RC522)
          - GND -> ESP32 **GND**
          - Wiper -> ESP32 **GPIO 34** (ADC1_CH6, input-only, WiFi-safe)
       6. Flash `firmware/esp32-rfid/esp32_rfid/esp32_rfid.ino` (Arduino IDE 2.x, ESP32 Dev Module, 115200 baud).
          See `firmware/esp32-rfid/LAB-SETUP-GUIDE.md` for the full lab procedure.
       
       ```

    2. AFTER the existing "## Step 6: Record the demo video" section (currently ends around line 116), and BEFORE the "## Latency measurement" heading at line 119, add a new section verbatim:

       ```
       
       ## Step 7 — Height Slider end-to-end smoke test (Phase 02.1)
       
       This is the goal-backward verification for Phase 02.1. Run it after flashing the firmware
       and re-pasting the four updated TD scripts (`serial_rfid_v1.py`, `vision2_state_chop.py`,
       `panel_text.py`, `footprint_viz_v5.py`).
       
       ### Setup
       
       1. ESP32-RFID flashed with the Phase 02.1 sketch (see firmware lab guide).
       2. Slider wired (VCC/GND/Wiper -> 3V3/GND/GPIO34).
       3. Arduino Serial Monitor closed (it locks the COM port).
       4. In TD: `rfid_in` is a **Serial DAT** (not the Constant CHOP stub), port = the ESP32's COM port, baud = 115200, Active = On.
       5. `compute_state` Script CHOP is the `vision2_state_chop.py` instance (NOT `state_chop_v1.py`).
       6. `stats_text` Text TOP (or whichever Text TOP is showing `bar_bottom_status()` content).
       
       ### Test A — Demo loop (slider sweep)
       
       1. Drag the slider slowly from one end to the other in ~3 s.
       2. **TD textport:** expect `[serial_rfid] floor=N` lines as N transitions through 1 -> 2 -> 3 -> 4 -> 5.
       3. **compute_state CHOP:** the `floor` channel should follow within 100 ms of each `FLOOR:` line. Watch it in the CHOP info panel (right-click -> View).
       4. **Stats overlay (bar_bottom_status):** the rightmost segment should change from `FLOOR 1` to `FLOOR 5` as you sweep. Latency target: < 100 ms.
       
       **Pass criterion:** all 5 floors observable in textport + CHOP + stats overlay during one full sweep.
       
       ### Test B — Jitter test (boundary hold)
       
       1. Find the slider position where `FLOOR:` just transitioned (e.g. between floor 2 and floor 3).
       2. Park the slider there and hold for **>= 5 seconds** without moving it.
       3. **TD textport:** expect ZERO additional `[serial_rfid] floor=N` lines during the hold.
       4. **compute_state CHOP:** the `floor` channel must NOT flicker between two values during the hold.
       5. **Stats overlay:** the `FLOOR N` segment must remain constant.
       
       **Pass criterion:** no oscillation in textport / CHOP / overlay during the 5-second hold. If it oscillates, increase `HYST_EPSILON` (firmware) or shorten `EMA_ALPHA` (firmware) — see Plan 01 SUMMARY for the location.
       
       ### Test C — Floor-cap test (per-method INVALID)
       
       1. Place the 3D PRINTED RFID tag on the reader. Expect `[serial_rfid] tag=... method_id=2 (3D PRINTED)` in textport.
       2. Stats overlay should show `METHOD 3D PRINTED`.
       3. Slide the slider above the floor-1 position so `floor` reaches 2 or higher.
       4. **render_footprint:** the `method_selection` panel should gain a **red border + diagonal red X** overlay (the floor-cap INVALID visual added in Plan 02 Task 4).
       5. Slide back down so `floor` returns to 1. The red border + X must disappear.
       6. Place a MASONRY tag (cap = 5). Sweep through all 5 floors. NO red border + X should appear at any floor.
       
       **Pass criterion:** red border + X appears exactly when `floor > methods_db.json[method_id].max_floors` and disappears otherwise.
       
       ```

    Do NOT modify the existing Prerequisites, Step 1, Step 2, Step 3, Step 4, Step 5 visual-state test table, the recording instructions in Step 6, the Latency measurement section, the Troubleshooting table, or the Files table.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='touchdesigner/VERTICAL-SLICE-RUNBOOK.md'; $c=Get-Content -Raw $f; if (-not ($c -match '### Height Slider \(Phase 02\.1')) { throw 'Hardware Setup slider sub-section missing' }; if (-not ($c -match 'GPIO 34.*ADC1_CH6')) { throw 'GPIO 34 wiring line missing' }; if (-not ($c -match '## Step 7 — Height Slider end-to-end smoke test')) { throw 'Step 7 heading missing' }; if (-not ($c -match '### Test A — Demo loop \(slider sweep\)')) { throw 'Test A heading missing' }; if (-not ($c -match '### Test B — Jitter test \(boundary hold\)')) { throw 'Test B heading missing' }; if (-not ($c -match '### Test C — Floor-cap test')) { throw 'Test C heading missing' }; if (-not ($c -match 'red border \+ diagonal red X')) { throw 'INVALID visual description missing' }; if (-not ($c -match '5 floors observable')) { throw 'Test A pass criterion missing' }; if (-not ($c -match 'no oscillation in textport')) { throw 'Test B pass criterion missing' }; if (-not ($c -match 'floor > methods_db\.json\[method_id\]\.max_floors')) { throw 'Test C pass criterion missing' }; if (-not ($c -match '## Latency measurement')) { throw 'Latency measurement section disturbed' }; if (-not ($c -match '## Troubleshooting')) { throw 'Troubleshooting section disturbed' }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Height Slider (Phase 02.1" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` >= 1.
    - `grep -c "## Step 7 — Height Slider end-to-end smoke test" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` == 1.
    - `grep -c "### Test A — Demo loop" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` == 1.
    - `grep -c "### Test B — Jitter test" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` == 1.
    - `grep -c "### Test C — Floor-cap test" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` == 1.
    - `grep -c "GPIO 34" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` >= 1.
    - `grep -c "5 floors observable" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` >= 1.
    - `grep -c "no oscillation in textport" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` >= 1.
    - `grep -c "## Latency measurement" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` == 1 (existing section intact).
    - `grep -c "## Troubleshooting" touchdesigner/VERTICAL-SLICE-RUNBOOK.md` == 1 (existing section intact).
  </acceptance_criteria>
  <done>Runbook has explicit wiring step + three named smoke-tests (sweep / jitter / cap) with pass criteria.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document new compute_state slider channels + Serial DAT storage in TD-FRAMEWORK-GUIDE.md</name>
  <files>touchdesigner/TD-FRAMEWORK-GUIDE.md</files>
  <read_first>
    - touchdesigner/TD-FRAMEWORK-GUIDE.md (node naming table at lines 9-21, Step 3 compute_state at lines 75-87, "When the ESP32 + RFID hardware arrives" at lines 187-195)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (TouchDesigner parsing + state aggregation blocks)
  </read_first>
  <action>
    Edit `touchdesigner/TD-FRAMEWORK-GUIDE.md`.

    1. In the Step 3 (`compute_state` Script CHOP) section, find the line "Outputs 4 channels: `puck_count`, `area_px2`, `method_id`, `hb_alive`" (currently line 83) AND the blockquote below it ("> Reads `op('vision_in')` and `op('rfid_in')` by name..."). Replace BOTH with the expanded block verbatim:

       ```
       Outputs (current canonical `vision2_state_chop.py` — see [`scripts/vision2_state_chop.py`](scripts/vision2_state_chop.py)):
       
       - **OSC-derived:** `puck_count`, `area_px2`, `area_m2`, `hb_alive`, `sketch_*`, `gesture_*`, `fsm_state`
       - **RFID-derived:** `method_id` (from `rfid_in` Serial DAT storage)
       - **Slider-derived (Phase 02.1):** `floor`, `slider_raw`, `slider_alive`
       
       > Reads `op('vision_in')` and `op('rfid_in')` by name — make sure both exist.
       > Slider channels read from `op('rfid_in').fetch('floor'|'slider_raw'|'slider_last_frame', default)`.
       > Staleness timeout: `SLIDER_TIMEOUT_TICKS = 60` frames (~2 s @ 30 fps).
       ```

    2. In the "## When the ESP32 + RFID hardware arrives" section (currently lines 187-195), AFTER step 5 (last line of the numbered list, "5. `compute_state` works with both — no script changes needed") at line 193, append a new sub-section verbatim:

       ```
       
       ### Slider (Phase 02.1)
       
       The DollaTek 10K linear-slide potentiometer is wired to the SAME ESP32-RFID board on **GPIO 34** (ADC1_CH6).
       It uses the same Serial DAT (`rfid_in`) — no second node, no second Serial port. The firmware emits two new
       line shapes alongside the existing `RFID:` / `HB:` / `BOOT:` lines:
       
       - `FLOOR:<int>` — quantized floor 1..MAX_FLOORS (firmware default 5), emitted only on change
       - `SLIDER:<0.xxx>` — smoothed normalized 0..1, emitted every ~200 ms
       
       Parsing (in `serial_rfid_v1.py`) stores each into the Serial DAT under named keys:
       
       | Storage key | Type | Set by |
       |---|---|---|
       | `method_id` | int | `RFID:` branch (existing) |
       | `floor` | int | `FLOOR:` branch (Phase 02.1) |
       | `slider_raw` | float | `SLIDER:` branch (Phase 02.1) |
       | `slider_last_frame` | int (TD frame) | `SLIDER:` branch (Phase 02.1) — used for `slider_alive` staleness |
       
       `vision2_state_chop.py` reads these via `op('rfid_in').fetch(key, default)` and surfaces three CHOP channels
       (`floor`, `slider_raw`, `slider_alive`). The HEIGHT FSM state (Phase 3) consumes `floor` directly via
       `op('compute_state')['floor'][0]`; per-method caps from `data/methods_db.json` `max_floors` decide when the
       floor-cap INVALID visual fires.
       
       ```

    Do NOT modify the architecture diagram, Steps 0-2, Step 4, Step 5, Step 6, Step 7, the testing-without-hardware section, or the adding-a-new-method section.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='touchdesigner/TD-FRAMEWORK-GUIDE.md'; $c=Get-Content -Raw $f; if (-not ($c -match 'Slider-derived \(Phase 02\.1\)')) { throw 'compute_state outputs block not updated' }; if (-not ($c -match 'SLIDER_TIMEOUT_TICKS = 60')) { throw 'staleness timeout note missing' }; if (-not ($c -match '### Slider \(Phase 02\.1\)')) { throw 'Slider sub-section missing in ESP32 arrival section' }; if (-not ($c -match 'GPIO 34')) { throw 'GPIO 34 missing in framework guide' }; if (-not ($c -match '`FLOOR:<int>`')) { throw 'FLOOR line spec missing' }; if (-not ($c -match '`SLIDER:<0\.xxx>`')) { throw 'SLIDER line spec missing' }; if (-not ($c -match 'slider_last_frame')) { throw 'slider_last_frame table row missing' }; if (-not ($c -match \"op\('compute_state'\)\['floor'\]\[0\]\")) { throw 'compute_state floor consumer reference missing' }; if (-not ($c -match '## Step 3 — `compute_state` \(Script CHOP\)')) { throw 'Step 3 heading damaged' }; if (-not ($c -match '## Testing without hardware')) { throw 'Testing section damaged' }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Slider-derived (Phase 02.1)" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "SLIDER_TIMEOUT_TICKS = 60" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "### Slider (Phase 02.1)" touchdesigner/TD-FRAMEWORK-GUIDE.md` == 1.
    - `grep -c "GPIO 34" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "FLOOR:<int>" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "SLIDER:<0.xxx>" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "slider_last_frame" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "op('compute_state')\['floor'\]\[0\]" touchdesigner/TD-FRAMEWORK-GUIDE.md` >= 1.
    - `grep -c "## Step 3 — `compute_state`" touchdesigner/TD-FRAMEWORK-GUIDE.md` == 1 (heading intact).
    - `grep -c "## Testing without hardware" touchdesigner/TD-FRAMEWORK-GUIDE.md` == 1 (section intact).
  </acceptance_criteria>
  <done>Framework guide documents compute_state slider channels, Serial DAT storage keys, GPIO34 pin, and HEIGHT FSM consumer pattern.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the three Phase 02.1 smoke tests end-to-end and confirm pass criteria (sweep + jitter + cap)</name>
  <files>touchdesigner/VERTICAL-SLICE-RUNBOOK.md</files>
  <read_first>
    - touchdesigner/VERTICAL-SLICE-RUNBOOK.md Step 7 (Phase 02.1 smoke test section added by Task 1 of this plan)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (Verification path block — demo loop, jitter test, cap test pass criteria)
    - .planning/phases/02.1-height-slider/02.1-01-SUMMARY.md (firmware tuning record from Plan 01)
    - .planning/phases/02.1-height-slider/02.1-02-SUMMARY.md (TD wiring record from Plan 02)
    - .planning/phases/02.1-height-slider/02.1-03-SUMMARY.md (data + contracts record from Plan 03)
  </read_first>
  <action>
    Human-verify checkpoint. Claude STOPS here and waits for the user to run the three smoke tests defined in `VERTICAL-SLICE-RUNBOOK.md` Step 7.

    Procedure for the user (mirrors `<how-to-verify>` below):

    1. Confirm Plan 01 firmware is flashed and approved (Plan 01 Task 3 checkpoint passed).
    2. Confirm all four Plan 02 TD scripts are re-pasted into their Script DAT/CHOP/TOP nodes.
    3. Confirm Plan 03 data/contracts are committed (`max_floors` in `methods_db.json`, etc.).
    4. Run Test A (slider sweep), Test B (jitter / boundary hold >=5 s), Test C (floor-cap with 3D PRINTED + MASONRY) per the runbook.
    5. Type the resume signal (`all three tests pass` or a per-test failure description).
  </action>
  <what-built>
    Full Phase 02.1 pipeline: firmware (Plan 01) + TD scripts (Plan 02) + data/contracts (Plan 03) + runbook (this plan). End-to-end: physical slider movement -> ESP32 ADC -> median+EMA+hysteresis -> Serial `FLOOR:N`/`SLIDER:0.xxx` lines -> Serial DAT storage -> compute_state channels -> stats overlay text + floor-cap visual.
  </what-built>
  <how-to-verify>
    Follow `touchdesigner/VERTICAL-SLICE-RUNBOOK.md` Step 7 verbatim. Three tests, three pass criteria:

    **Test A — Demo loop (slider sweep)**
    1. Slowly sweep slider from min to max in ~3 seconds.
    2. Confirm TD textport prints `[serial_rfid] floor=1` through `[serial_rfid] floor=5` in order.
    3. Confirm `compute_state` CHOP shows the same value on its `floor` channel within ~100 ms of each printed line.
    4. Confirm the stats overlay's `FLOOR` segment cycles from `FLOOR 1` to `FLOOR 5`.

    **Pass A:** all 5 floors observable in all 3 surfaces.

    **Test B — Jitter test (boundary hold)**
    1. Park the slider at a known floor boundary (e.g. between FLOOR:2 and FLOOR:3).
    2. Hold for >=5 seconds.
    3. Confirm ZERO additional `[serial_rfid] floor=...` lines during the hold.
    4. Confirm the compute_state `floor` channel stays at one value, no flicker.

    **Pass B:** no oscillation during the 5 s hold.

    **Test C — Floor-cap test (per-method INVALID)**
    1. Scan the 3D PRINTED RFID tag. Confirm `METHOD 3D PRINTED` in the stats overlay and `method_id=2` in the textport.
    2. Slide above floor 1 (so floor reaches 2..5).
    3. Confirm the `method_selection` panel gets a **red border + diagonal red X** overlay.
    4. Slide back to floor 1. Confirm the red overlay disappears.
    5. Scan MASONRY tag (cap=5). Sweep through all 5 floors. Confirm NO red overlay appears.

    **Pass C:** red border + X appears exactly when `floor > max_floors[method_id]`, disappears otherwise.

    **If any test fails:** note which surface (textport / CHOP / stats overlay / render_footprint) was wrong, screenshot if useful, and describe the symptom. We will diagnose and update the relevant plan (most likely Plan 01 hysteresis tuning or Plan 02 fetch wiring).
  </how-to-verify>
  <verify>
    Human verification (no automated command — this is a hardware-in-loop check). The user runs Tests A/B/C from `VERTICAL-SLICE-RUNBOOK.md` Step 7 and confirms all three pass criteria.
  </verify>
  <acceptance_criteria>
    - User confirms Pass A: all 5 floors (FLOOR:1..FLOOR:5) observable across textport + CHOP + stats overlay during one sweep.
    - User confirms Pass B: holding slider at a boundary for >=5 s yields ZERO additional FLOOR lines and the `floor` channel does NOT flicker.
    - User confirms Pass C: with 3D PRINTED (cap=1), red border + X appears when floor > 1 and disappears when floor returns to 1; with MASONRY (cap=5), red border + X NEVER appears.
    - Measured slider->overlay latency < 100 ms (per the runbook's Test A pass criterion).
  </acceptance_criteria>
  <done>User types `all three tests pass` (Phase 02.1 closed) OR provides per-test failure descriptions that become inputs for `/gsd:plan-phase 02.1 --gaps`.</done>
  <resume-signal>Type "all three tests pass" to close Phase 02.1. Otherwise type "Test {A|B|C} failed: {symptom}" so the failure can be turned into a gap-closure plan (`/gsd:plan-phase 02.1 --gaps`).</resume-signal>
</task>

</tasks>

<verification>
- Both updated docs contain the strings asserted in acceptance_criteria.
- VERTICAL-SLICE-RUNBOOK.md Step 7 covers all three CONTEXT.md verification-path scenarios (demo loop, jitter test, cap test).
- TD-FRAMEWORK-GUIDE.md compute_state Step 3 lists the three new slider channels.
- Smoke test executes end-to-end with all 3 pass criteria met (final checkpoint).
</verification>

<success_criteria>
- ROADMAP Phase 02.1 success criterion #6 satisfied (demo path proven end-to-end within 100 ms, no jitter at boundaries).
- Smoke-test runbook is reproducible by any team member without additional context.
- Phase 02.1 ready to close — STATE.md gets a "Phase 02.1 complete" line in the SUMMARY for this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-height-slider/02.1-04-SUMMARY.md` per the standard template, documenting: smoke-test results (pass/fail per test A/B/C), any firmware tuning deviations (`EMA_ALPHA` / `HYST_EPSILON` adjustments observed during jitter test), measured slider->overlay latency, and a one-line phase-close note for STATE.md.
</output>
