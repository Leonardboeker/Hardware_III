---
phase: 02.1-height-slider
plan: 03
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - data/methods_db.json
  - data/README.md
  - INTERFACE_CONTRACT.md
  - .planning/FSM_TOUCHDESIGNER_SPEC.md
  - .planning/PROJECT.md
  - firmware/esp32-rfid/README.md
  - firmware/esp32-rfid/LAB-SETUP-GUIDE.md
autonomous: true
requirements:
  - INP-03
must_haves:
  truths:
    - "Every method in methods_db.json has an integer `max_floors` field (validated)"
    - "INTERFACE_CONTRACT.md Section 9 lists `floor`, `slider_raw`, `slider_online` as new sensor fields and the `FLOOR:`/`SLIDER:` Serial line shapes"
    - "INTERFACE_CONTRACT.md Section 4 lists `floor`, `slider_raw`, `slider_alive` as new compute_state channels"
    - "FSM_TOUCHDESIGNER_SPEC.md HEIGHT row entry condition cites the Serial `floor` channel (not ArUco dial ID-10)"
    - "PROJECT.md Input/Sensing block names the DollaTek slider and Layered State Model HEIGHT bullet drops the ArUco-dial-10 reference"
    - "firmware/esp32-rfid/README.md wiring table includes a Slider row (3V3 / GND / GPIO34)"
    - "firmware/esp32-rfid/LAB-SETUP-GUIDE.md has a slider hookup step with the GPIO34 pin and ASCII diagram update"
  artifacts:
    - path: "data/methods_db.json"
      provides: "max_floors per method"
      contains: "max_floors"
    - path: "INTERFACE_CONTRACT.md"
      provides: "Slider/floor sensor contract"
      contains: "FLOOR:"
    - path: ".planning/FSM_TOUCHDESIGNER_SPEC.md"
      provides: "Updated HEIGHT row"
      contains: "Serial floor channel"
    - path: "firmware/esp32-rfid/README.md"
      provides: "Slider wiring row"
      contains: "GPIO 34"
  key_links:
    - from: "methods_db.json"
      to: "footprint_viz_v5.py _max_floors_for"
      via: "JSON field `max_floors` on each method"
      pattern: "max_floors"
    - from: "INTERFACE_CONTRACT.md Section 9"
      to: "ESP32 firmware emit lines"
      via: "Documented FLOOR:/SLIDER: shapes"
      pattern: "FLOOR:|SLIDER:"
---

<objective>
Codify Phase 02.1 in the project's interface contract, FSM spec, project doc, and per-method data. Every reference that still says "ArUco dial ID-10" must point to the Serial `floor` channel instead. Every method in `data/methods_db.json` gets an integer `max_floors` field. The firmware README and lab guide gain a Slider wiring row + hookup step.

Purpose: Phase 3 FSM wiring + Plan 02's TD `_max_floors_for` helper depend on the JSON field + spec text being canonical. Without this, downstream consumers have nothing authoritative to read.
Output: 7 doc/data files updated. No code touched in this plan (code paths live in Plan 01 + Plan 02).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.1-height-slider/02.1-CONTEXT.md
@.planning/phases/02.1-height-slider/02.1-01-firmware-slider-adc-PLAN.md
@INTERFACE_CONTRACT.md
@.planning/FSM_TOUCHDESIGNER_SPEC.md
@.planning/PROJECT.md
@data/methods_db.json
@data/README.md
@firmware/esp32-rfid/README.md
@firmware/esp32-rfid/LAB-SETUP-GUIDE.md

<interfaces>
<!-- Locked per-method max_floors values from CONTEXT.md decisions block: -->
masonry          (id 1): 5
3d_printed       (id 2): 1   // earth 3DP locked to 1 story (PROJECT.md success criterion)
prefab           (id 3): 4
reclaimed_brick  (id 4): 5   // baseline; inherits active method's cap, documented in JSON note
NONE             (id 0): 5   // safe default, never actually used (no method selected)

<!-- Existing INTERFACE_CONTRACT.md Section 9 fields (lines 380-395):
     user_present, proximity_value, reset_signal, sensor_online
     This plan adds: floor (int, 1..N), slider_raw (float, 0..1), slider_online (bool)
     And documents the new Serial line prefixes: FLOOR:<int>, SLIDER:<0.xxx>
     alongside existing RFID:<HEX>, HB:<sec>, BOOT:rfid_reader_ready -->

<!-- Existing FSM_TOUCHDESIGNER_SPEC.md HEIGHT row (Section 4, line 101):
     HEIGHT | Sets building height / number of floors | Footprint is complete | Height marker is confirmed in target | MATERIALS | PENDING, INVALID, VALID
     This plan rewrites the "Entry condition" + "Success condition" cells to cite the Serial floor channel
     and adds a footnote about the per-method max_floors gate. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `max_floors` integer field to every method in data/methods_db.json + extend data/README.md with a max_floors line</name>
  <files>data/methods_db.json, data/README.md</files>
  <read_first>
    - data/methods_db.json (so executor sees the existing method object shape — id 0..4, all keys preserved)
    - data/README.md (so executor sees the existing field-rules block)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (Data block — locked per-method values)
  </read_first>
  <action>
    Step A — edit `data/methods_db.json`.

    For EACH method object in the `methods` array, add a single integer key `"max_floors"` immediately AFTER the `"max_pucks"` line (keeps related "limits" keys grouped). Use these exact values:

    | method.id | method.name        | max_floors |
    |-----------|--------------------|-----------:|
    | 0         | NONE               | 5          |
    | 1         | MASONRY            | 5          |
    | 2         | 3D PRINTED         | 1          |
    | 3         | PREFAB             | 4          |
    | 4         | RECLAIMED BRICK    | 5          |

    Concretely, for the MASONRY object (currently starts at line 39, has `"max_pucks": 10,` at line 44), the edit inserts:

    ```json
          "max_pucks": 10,
          "max_floors": 5,
    ```

    Apply the same insertion (correct value per the table) to all five method objects (ids 0, 1, 2, 3, 4). Do NOT reorder any other keys. Do NOT touch the top-level `_note`, `_metric_engine_notes`, `shape_factor_table`, `phases`, or `comparison_insight` blocks. Output must still parse as valid JSON.

    ALSO append a brief comment line to the top-level `_note` field (the existing string at lines 2): leave that string unchanged. Instead, add a new top-level key `"_phase_02_1_note"` directly AFTER the `_metric_engine_notes` block (so it stays adjacent to the data-shape notes). Value verbatim:

    ```json
      "_phase_02_1_note": "max_floors is per-method ceiling for HEIGHT state. Slider quantizes 1..MAX_FLOORS_firmware (=5); if floor > methods_db.json[method_id].max_floors, FSM emits INVALID. 3D PRINTED is locked to 1 (earth 3DP max 1 story — see .planning/PROJECT.md). RECLAIMED BRICK is baseline only, treated as cap=5 in case it is ever activated standalone.",
    ```

    (Keep the trailing comma — the next key is `"shape_factor_table"`.)

    Step B — edit `data/README.md`.

    In the "Schema" section, immediately AFTER the unified-header bullet block (currently lines 28-32), and BEFORE the "Field rules:" line (currently line 34), add a new short paragraph verbatim:

    ```markdown

    ### methods_db.json — runtime method table

    The `data/methods_db.json` file lives next to the per-method CSVs and contains the
    runtime-facing method table that TouchDesigner reads directly. Each method object in
    the `methods` array carries the LCA summary ranges plus a small set of runtime caps:

    | Field | Type | Purpose |
    |---|---|---|
    | `min_pucks` / `max_pucks` | int | Footprint puck count bounds |
    | `max_floors` | int | HEIGHT-state cap (slider in 1..N quantized space; FSM emits INVALID above this) |
    | `rfid_tag` | string | UID mapped to this method (registered via firmware/esp32-rfid/TAG-REGISTRY.md) |
    | `color_rgb` | array | Method color used by `footprint_viz_v5.py` METHOD_COLORS |

    ```

    Do NOT modify any other section of `data/README.md`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='data/methods_db.json'; $raw=Get-Content -Raw $f; $json = $raw | ConvertFrom-Json; $methods = $json.methods; if ($methods.Count -lt 5) { throw 'methods array shorter than expected' }; $expected = @{0=5; 1=5; 2=1; 3=4; 4=5}; foreach ($m in $methods) { $id = [int]$m.id; if (-not $m.PSObject.Properties.Name -contains 'max_floors') { throw \"method id=$id missing max_floors\" }; $got = [int]$m.max_floors; $want = [int]$expected[$id]; if ($got -ne $want) { throw \"method id=$id max_floors=$got expected $want\" } }; if (-not $json.PSObject.Properties.Name.Contains('_phase_02_1_note')) { throw '_phase_02_1_note missing' }; $r='data/README.md'; $rc=Get-Content -Raw $r; if (-not ($rc -match '### methods_db.json — runtime method table')) { throw 'README section header missing' }; if (-not ($rc -match '`max_floors`')) { throw 'max_floors row missing from README' }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `data/methods_db.json` parses as valid JSON (`python -c "import json; json.load(open('data/methods_db.json'))"` exits 0).
    - Every method object in `methods_db.json` has an integer `max_floors` key.
    - Specific values: id 0 → 5, id 1 → 5, id 2 → 1, id 3 → 4, id 4 → 5.
    - `grep -c '"max_floors"' data/methods_db.json` == 5.
    - `grep -c '"max_floors": 1' data/methods_db.json` == 1 (only 3D PRINTED).
    - `grep -c '"max_floors": 4' data/methods_db.json` == 1 (only PREFAB).
    - `grep -c '"_phase_02_1_note"' data/methods_db.json` == 1.
    - `data/methods_db.json` still contains `"rfid_tag": "0430BF011F5713"` (masonry tag) — existing fields preserved.
    - `data/methods_db.json` still contains `"shape_factor_table"` and `"phases"` top-level keys.
    - `grep -c "### methods_db.json — runtime method table" data/README.md` == 1.
    - `grep -c "max_floors.*HEIGHT-state cap" data/README.md` >= 1.
  </acceptance_criteria>
  <done>Every method has an integer `max_floors`, JSON validates, README documents the field.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend INTERFACE_CONTRACT.md Section 9 (Sensor/ESP32) with floor/slider fields + Serial line prefixes; extend Section 4 with new compute_state channels</name>
  <files>INTERFACE_CONTRACT.md</files>
  <read_first>
    - INTERFACE_CONTRACT.md (full file — Section 4 starts at line 113, Section 9 starts at line 378)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (Contracts and specs block — locked field names + Serial prefix list)
  </read_first>
  <action>
    Edit `INTERFACE_CONTRACT.md`.

    Step A — Section 9 (Sensor / ESP32 Input Contract). Currently lines 378-400.

    1. After the existing "### Fields" bullet list (currently lines 380-384), keep the four existing bullets (`user_present`, `proximity_value`, `reset_signal`, `sensor_online`) and APPEND three new bullets verbatim:

       ```markdown
       - `floor`
       - `slider_raw`
       - `slider_online`
       ```

    2. Update the JSON example (currently lines 388-395). The example currently shows four fields. Replace the entire example block verbatim with:

       ````markdown
       ```json
       {
         "user_present": true,
         "proximity_value": 0.78,
         "reset_signal": false,
         "sensor_online": true,
         "floor": 3,
         "slider_raw": 0.512,
         "slider_online": true
       }
       ```
       ````

    3. After the existing "### What they do" bullet block (currently lines 397-400), keep the three existing bullets and APPEND three new bullets verbatim:

       ```markdown
       - `floor` is the quantized HEIGHT-state input (1..N). The FSM reads this directly; the ArUco-Dial ID-10 path is retired
       - `slider_raw` is the smoothed normalized [0..1] slider reading. Debug / diagnostic only; not used by the content FSM
       - `slider_online` is a health flag; goes false when no `SLIDER:` tick has arrived within ~2 s
       ```

    4. AFTER the "### What they do" section's last bullet (the new `slider_online` line), add a new sub-section verbatim. This documents the actual Serial line shapes:

       ````markdown

       ### Current live messages (USB Serial @115200, ESP32 → TouchDesigner)

       | Line prefix | Args | Meaning |
       |---|---|---|
       | `BOOT:rfid_reader_ready` | none | Firmware booted, RC522 + slider both initialized |
       | `HB:<sec>` | seconds since boot | Heartbeat — one device, one heartbeat (covers RFID + slider liveness) |
       | `RFID:<HEX>` | uppercase tag UID | New tag scan (debounced REREAD_MS=1500) |
       | `FLOOR:<int>` | quantized floor 1..N | Emitted only on quantized floor change (firmware MAX_FLOORS=5) |
       | `SLIDER:<0.xxx>` | smoothed normalized 0..1 | Emitted every ~200 ms — periodic alive tick + debug |

       Parsing lives in [`touchdesigner/scripts/serial_rfid_v1.py`](touchdesigner/scripts/serial_rfid_v1.py); each line stores into the Serial DAT (`rfid_in`) under named keys (`method_id`, `floor`, `slider_raw`, `slider_last_frame`).
       ````

    Step B — Section 4 (Current Working TouchDesigner Contract). Currently lines 113-145.

    AFTER the existing "### Current live messages" table (currently lines 134-140) and BEFORE the "### Why this matters" heading at line 142, add a new sub-section verbatim documenting the slider-side compute_state additions:

    ````markdown

    ### Serial → TouchDesigner storage (ESP32 RFID + Slider, Phase 02.1)

    From [`serial_rfid_v1.py`](touchdesigner/scripts/serial_rfid_v1.py) and [`vision2_state_chop.py`](touchdesigner/scripts/vision2_state_chop.py):

    ```python
    # Serial DAT (rfid_in) storage:
    method_id:         int    0 = NONE, 1 = MASONRY, 2 = 3D PRINTED, 3 = PREFAB, 4 = RECLAIMED BRICK
    floor:             int    1..MAX_FLOORS (firmware MAX_FLOORS = 5)
    slider_raw:        float  0.0..1.0 smoothed normalized value
    slider_last_frame: int    absTime.frame stamp from the last SLIDER: tick (for staleness)
    ```

    `compute_state` Script CHOP exposes these as named channels alongside the existing `puck_count`/`area_m2`/`method_id`/`hb_alive`/etc set:

    | Channel | Type | Source |
    |---|---|---|
    | `floor` | int | `op('rfid_in').fetch('floor', 1)` |
    | `slider_raw` | float | `op('rfid_in').fetch('slider_raw', 0.0)` |
    | `slider_alive` | int (0/1) | `(me.time.frame - slider_last_frame) < SLIDER_TIMEOUT_TICKS` |

    ````

    Do NOT modify Sections 1, 2, 3, 5, 6, 7, 8, 10, 11, 12, 13, 14, or the rest of Section 4 prior to the "### Why this matters" heading.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='INTERFACE_CONTRACT.md'; $c=Get-Content -Raw $f; if (-not ($c -match '- `floor`')) { throw 'floor field bullet missing' }; if (-not ($c -match '- `slider_raw`')) { throw 'slider_raw field bullet missing' }; if (-not ($c -match '- `slider_online`')) { throw 'slider_online field bullet missing' }; if (-not ($c -match '\"floor\": 3')) { throw 'floor in JSON example missing' }; if (-not ($c -match '\"slider_raw\": 0\\.512')) { throw 'slider_raw in JSON example missing' }; if (-not ($c -match 'Current live messages \\(USB Serial @115200')) { throw 'Section 9 live-messages subsection missing' }; if (-not ($c -match '`FLOOR:<int>`')) { throw 'FLOOR row missing' }; if (-not ($c -match '`SLIDER:<0\\.xxx>`')) { throw 'SLIDER row missing' }; if (-not ($c -match 'Serial → TouchDesigner storage')) { throw 'Section 4 Serial storage subsection missing' }; if (-not ($c -match 'slider_last_frame')) { throw 'slider_last_frame field missing' }; if (-not ($c -match '`slider_alive`.*int.*me\\.time\\.frame')) { throw 'slider_alive table row missing or malformed' }; if (-not ($c -match '## 9\\. Sensor / ESP32 Input Contract')) { throw 'Section 9 heading damaged' }; if (-not ($c -match '## 4\\. Current Working TouchDesigner Contract')) { throw 'Section 4 heading damaged' }; if (-not ($c -match '## 5\\. CV -> FSM Input Contract')) { throw 'Section 5 damaged' }; if (-not ($c -match '## 13\\. Minimum Prototype Contract')) { throw 'Section 13 damaged' }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `INTERFACE_CONTRACT.md` Section 9 contains the three new field bullets (`floor`, `slider_raw`, `slider_online`).
    - `grep -c '- `floor`' INTERFACE_CONTRACT.md` >= 1.
    - JSON example in Section 9 contains `"floor": 3`, `"slider_raw": 0.512`, `"slider_online": true`.
    - Section 9 has a "Current live messages (USB Serial @115200, ESP32 → TouchDesigner)" sub-section.
    - That sub-section's table lists 5 line shapes: `BOOT:`, `HB:`, `RFID:`, `FLOOR:`, `SLIDER:`.
    - `grep -c '`FLOOR:<int>`' INTERFACE_CONTRACT.md` >= 1.
    - `grep -c '`SLIDER:<0\\.xxx>`' INTERFACE_CONTRACT.md` >= 1.
    - Section 4 contains a "Serial → TouchDesigner storage" sub-section.
    - That sub-section lists `floor`, `slider_raw`, `slider_alive` as new compute_state channels.
    - All section headings preserved: `## 4.`, `## 5.`, `## 9.`, `## 13.` still present.
  </acceptance_criteria>
  <done>Interface contract sections 4 and 9 codify the new Serial lines, sensor fields, and compute_state channels.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update FSM_TOUCHDESIGNER_SPEC.md HEIGHT row + PROJECT.md sensing/state lines</name>
  <files>.planning/FSM_TOUCHDESIGNER_SPEC.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/FSM_TOUCHDESIGNER_SPEC.md (Section 4, HEIGHT row at line 101; Section 7 transition table at lines 162-173)
    - .planning/PROJECT.md (lines 40-50 — content FSM bullets; lines 78-83 — Input/Sensing block)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (Contracts and specs block)
  </read_first>
  <action>
    Step A — edit `.planning/FSM_TOUCHDESIGNER_SPEC.md`.

    1. Section 4 table (line 101). Replace the HEIGHT row verbatim:

       Currently:
       ```
       | `HEIGHT` | Sets building height / number of floors | Footprint is complete | Height marker is confirmed in target | `MATERIALS` | `PENDING`, `INVALID`, `VALID` |
       ```

       Replace with:
       ```
       | `HEIGHT` | Sets building height / number of floors | Footprint is complete | Serial `floor` channel from ESP32 slider holds value, validated against the active method's `max_floors` (data/methods_db.json) | `MATERIALS` | `PENDING`, `INVALID`, `VALID` |
       ```

    2. Section 7 transition table (line 169). Replace the existing HEIGHT row verbatim:

       Currently:
       ```
       | `HEIGHT` | Height marker is confirmed | `MATERIALS` |
       ```

       Replace with:
       ```
       | `HEIGHT` | Serial `floor` channel from ESP32 slider (1..max_floors for active method) | `MATERIALS` |
       ```

    3. AFTER the Section 8 "### Wrapper rules" bullet block (currently lines 188-190) and BEFORE the "### Visual rules" heading at line 192, add a short note verbatim:

       ```markdown

       ### HEIGHT input (Phase 02.1)
       - HEIGHT consumes `op('compute_state')['floor'][0]` (sourced from the DollaTek slider via the ESP32-RFID Serial pipe — see `firmware/esp32-rfid/esp32_rfid/esp32_rfid.ino` and `touchdesigner/scripts/serial_rfid_v1.py`)
       - Per-method cap from `data/methods_db.json` `max_floors`; when `floor > max_floors[method_id]` set visual `INVALID` and pause confirmation
       - ArUco dial ID-10 path retired from runtime (PNG/CAD asset archived under `cad/aruco-markers/aruco_id10_height.png`)

       ```

    Step B — edit `.planning/PROJECT.md`.

    1. Content FSM bullets (currently lines 43-50). Replace line 46 verbatim:

       Currently:
       ```
       - **HEIGHT**: User rotates ArUco dial (ID 10) → floors selected
       ```

       Replace with:
       ```
       - **HEIGHT**: User slides DollaTek 10K linear potentiometer (wired to ESP32-RFID GPIO34) → floor (1..max_floors) selected (per-method cap from data/methods_db.json; 3DP locked to 1)
       ```

    2. Input/Sensing block (currently lines 78-83). Add ONE new bullet at the END of that block (after the existing `Physical pieces` bullet at line 83) verbatim:

       ```markdown
       - **DollaTek 10K linear slide potentiometer** (HEIGHT input): wired to ESP32-RFID GPIO34, smoothed median + EMA + hysteresis in firmware, emits `FLOOR:N` (on change) and `SLIDER:0.xxx` (periodic) over USB Serial @115200 — same pipe as RFID method selection (no new WiFi path)
       ```

    Do NOT modify the locked-decisions block at the top of either file, the Building Phases table, the Toolkit list, or the canonical content FSM diagram line itself (line 40, with the `→` arrows — that is the visual FSM and is method-agnostic).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning/FSM_TOUCHDESIGNER_SPEC.md'; $c=Get-Content -Raw $f; if (-not ($c -match 'Serial `floor` channel from ESP32 slider holds value, validated against the active method')) { throw 'Section 4 HEIGHT row not updated' }; if ($c -match 'Height marker is confirmed in target') { throw 'Section 4 HEIGHT row still cites Height marker' }; if (-not ($c -match 'Serial `floor` channel from ESP32 slider \\(1\\.\\.max_floors')) { throw 'Section 7 HEIGHT row not updated' }; if (-not ($c -match '### HEIGHT input \\(Phase 02\\.1\\)')) { throw 'Section 8 HEIGHT input subsection missing' }; if (-not ($c -match \"op\\('compute_state'\\)\\['floor'\\]\\[0\\]\")) { throw 'compute_state floor reference missing in spec' }; if (-not ($c -match 'ArUco dial ID-10 path retired')) { throw 'retirement note missing' }; $p='.planning/PROJECT.md'; $pc=Get-Content -Raw $p; if (-not ($pc -match 'DollaTek 10K linear potentiometer')) { throw 'PROJECT.md HEIGHT bullet not updated' }; if ($pc -match 'User rotates ArUco dial \\(ID 10\\)') { throw 'PROJECT.md still has old HEIGHT bullet text' }; if (-not ($pc -match 'DollaTek 10K linear slide potentiometer.*HEIGHT input')) { throw 'PROJECT.md Input/Sensing bullet missing' }; if (-not ($pc -match 'GPIO34')) { throw 'GPIO34 pin reference missing in PROJECT.md' }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Serial \`floor\` channel from ESP32 slider holds value" .planning/FSM_TOUCHDESIGNER_SPEC.md` >= 1.
    - `grep -c "Height marker is confirmed in target" .planning/FSM_TOUCHDESIGNER_SPEC.md` == 0 (old text fully replaced).
    - `grep -c "### HEIGHT input (Phase 02.1)" .planning/FSM_TOUCHDESIGNER_SPEC.md` == 1.
    - `grep -c "ArUco dial ID-10 path retired" .planning/FSM_TOUCHDESIGNER_SPEC.md` == 1.
    - `grep -c "DollaTek 10K linear potentiometer" .planning/PROJECT.md` >= 1.
    - `grep -c "User rotates ArUco dial (ID 10)" .planning/PROJECT.md` == 0 (old bullet replaced).
    - `grep -c "GPIO34" .planning/PROJECT.md` >= 1.
    - Original locked-decisions sections in both files unchanged (use any pre-existing unique string as anchor — e.g. `Closed-loop CV from day one` in PROJECT.md still present once).
  </acceptance_criteria>
  <done>FSM spec HEIGHT row + PROJECT.md sensing block name the slider, retire the ArUco dial reference, and reference the methods_db max_floors cap.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add Slider row to firmware/esp32-rfid/README.md + LAB-SETUP-GUIDE.md hookup step</name>
  <files>firmware/esp32-rfid/README.md, firmware/esp32-rfid/LAB-SETUP-GUIDE.md</files>
  <read_first>
    - firmware/esp32-rfid/README.md (existing wiring table lines 10-21)
    - firmware/esp32-rfid/LAB-SETUP-GUIDE.md (existing hardware list lines 15-21, pinout table lines 33-44, ASCII diagram lines 47-60)
    - .planning/phases/02.1-height-slider/02.1-CONTEXT.md (Hardware block — wiring decisions)
  </read_first>
  <action>
    Step A — edit `firmware/esp32-rfid/README.md`.

    1. In the "Wiring" code block (currently lines 9-21), AFTER the existing `GND       -> GND` line (line 19) and BEFORE the closing triple backticks (line 21), insert the slider rows verbatim:

       ```
       
       Slider (DollaTek 10K)
       ─────────────────────────
       VCC       -> 3V3   (shares with RC522)
       GND       -> GND
       Wiper     -> GPIO 34   (ADC1_CH6, input-only, WiFi-safe)
       ```

       (The first inserted line is intentionally blank so the existing RC522 block and the new Slider block read as two grouped sections.)

    2. AFTER the existing "## Test (without TouchDesigner)" section's monospace block (currently lines 39-47) and BEFORE the "The `BOOT` line confirms..." sentence at line 49, append two new lines verbatim:

       ```
       SLIDER:0.000
       SLIDER:0.123    ← appears every ~200 ms while slider moves
       FLOOR:2         ← appears only on quantized floor change (1..5)
       ```

       Also update the explanation paragraph at line 49 to mention the new lines. Replace it verbatim with:

       ```
       The `BOOT` line confirms firmware loaded. `HB:N` is a per-second heartbeat so
       TD knows the device is alive even with no input. `RFID:<HEX>` is the tag UID.
       `SLIDER:0.xxx` is the smoothed slider position (0..1) emitted every ~200 ms.
       `FLOOR:N` appears only when the quantized floor (1..MAX_FLOORS, firmware default 5)
       changes — see `esp32_rfid.ino` constants `SLIDER_PIN`, `MAX_FLOORS`, `EMA_ALPHA`,
       `HYST_EPSILON` for the smoothing details.
       ```

    Step B — edit `firmware/esp32-rfid/LAB-SETUP-GUIDE.md`.

    1. In Section 1 hardware checklist (currently lines 15-21), AFTER the existing `MIFARE Classic 1K Tags (S50)` line at line 17, add a new checklist bullet verbatim:

       ```markdown
       - [ ] **DollaTek 10K Linear Slide Potentiometer Module** × 1 (Amazon ASIN B07HBWQGYP) — 3-pin breadboard module for HEIGHT input
       ```

    2. In Section 2 Pinout-Tabelle (currently lines 35-44), AFTER the existing `IRQ` row (line 44) and BEFORE the next section heading at line 46 (`### ASCII-Diagramm`), append three new table rows verbatim. Use the same German `Farbe` column convention:

       ```markdown
       | **— Slider —** | | | | |
       | `VCC` | DollaTek 3.3V | → | **3V3** (gleicher Rail wie RC522) | Rot |
       | `GND` | DollaTek GND | → | **GND** | Schwarz |
       | `Wiper` | DollaTek Analog out | → | **GPIO 34** (ADC1_CH6, input-only) | Orange |
       ```

    3. In Section 2 ASCII-Diagramm (currently lines 47-60), AFTER the existing diagram closing line (the `└──────────┘` `└─────────┘` line at line 59), and BEFORE the `### Reihenfolge zum Verkabeln` heading at line 62, add a new diagram block verbatim:

       ```
       
                  Slider (DollaTek)                       ESP32
              ┌──────────────────┐                      ┌─────────┐
              │  VCC   ●────────┼──────────ROT─────────●─┤ 3V3     │
              │  Wiper ●────────┼──────────ORANGE──────●─┤ GPIO 34 │
              │  GND   ●────────┼──────────SCHWARZ─────●─┤ GND     │
              └──────────────────┘                      └─────────┘
       ```

    4. In Section 5 (Test im Serial Monitor), AFTER the existing expected-output block (currently lines 138-146 — the `BOOT:rfid_reader_ready` / `HB:1`-`HB:3` block), and BEFORE the "Tag auf den Reader legen" step at line 147, add a new step verbatim:

       ```markdown
       
       4a. **Slider bewegen** (auf dem Modul von einem Ende zum anderen schieben).
           Erwartet zwischen den `HB:` / `SLIDER:` Ticks:
           ```
           SLIDER:0.000
           SLIDER:0.123
           SLIDER:0.250
           FLOOR:2
           SLIDER:0.375
           SLIDER:0.500
           FLOOR:3
           SLIDER:0.625
           ...
           FLOOR:5
           ```
           - `SLIDER:0.xxx` Zeile alle ~200 ms (Heartbeat-für-Slider)
           - `FLOOR:N` nur bei Übergang zwischen quantisierten Etagen 1..5
           - Wenn der Slider an einer Grenze stehen bleibt, soll `FLOOR:N` **NICHT** oszillieren (Hysterese arbeitet)
       
       ```

       (The existing step 4 "Tag auf den Reader legen" must remain as the next step; the new step is inserted between the expected-output and step 4.)
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='firmware/esp32-rfid/README.md'; $c=Get-Content -Raw $f; if (-not ($c -match 'Slider \\(DollaTek 10K\\)')) { throw 'Slider section in README wiring missing' }; if (-not ($c -match 'GPIO 34')) { throw 'GPIO 34 missing in README' }; if (-not ($c -match 'SLIDER:0\\.123')) { throw 'SLIDER example line missing' }; if (-not ($c -match 'FLOOR:2')) { throw 'FLOOR example missing' }; if (-not ($c -match 'SLIDER_PIN.*MAX_FLOORS.*EMA_ALPHA')) { throw 'README explanation paragraph not updated' }; $l='firmware/esp32-rfid/LAB-SETUP-GUIDE.md'; $lc=Get-Content -Raw $l; if (-not ($lc -match 'DollaTek 10K Linear Slide Potentiometer Module')) { throw 'LAB-SETUP hardware bullet missing' }; if (-not ($lc -match 'B07HBWQGYP')) { throw 'Amazon ASIN missing' }; if (-not ($lc -match '\\*\\*— Slider —\\*\\*')) { throw 'Slider table separator row missing' }; if (-not ($lc -match 'GPIO 34.*ADC1_CH6')) { throw 'GPIO 34 pin row missing in LAB-SETUP' }; if (-not ($lc -match 'Slider \\(DollaTek\\)')) { throw 'ASCII diagram heading missing' }; if (-not ($lc -match 'Slider bewegen')) { throw 'Lab-setup slider test step missing' }; if (-not ($lc -match 'Hysterese arbeitet')) { throw 'Hysterese note missing' }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Slider (DollaTek 10K)" firmware/esp32-rfid/README.md` >= 1.
    - `grep -c "GPIO 34" firmware/esp32-rfid/README.md` >= 1.
    - `grep -c "SLIDER:0.123" firmware/esp32-rfid/README.md` >= 1.
    - `grep -c "FLOOR:2" firmware/esp32-rfid/README.md` >= 1.
    - `grep -c "DollaTek 10K Linear Slide Potentiometer Module" firmware/esp32-rfid/LAB-SETUP-GUIDE.md` >= 1.
    - `grep -c "B07HBWQGYP" firmware/esp32-rfid/LAB-SETUP-GUIDE.md` >= 1.
    - `grep -c "Slider bewegen" firmware/esp32-rfid/LAB-SETUP-GUIDE.md` >= 1.
    - `grep -c "Hysterese arbeitet" firmware/esp32-rfid/LAB-SETUP-GUIDE.md` >= 1.
    - Existing `RC522 SDA` row still present in both files (existing rows intact).
    - `grep -c "GPIO 34" firmware/esp32-rfid/LAB-SETUP-GUIDE.md` >= 1.
  </acceptance_criteria>
  <done>Firmware README + lab guide name the slider, document GPIO34 wiring, expected Serial output, and the slider sweep test step.</done>
</task>

</tasks>

<verification>
- `python -c "import json; json.load(open('data/methods_db.json'))"` exits 0; `max_floors` count == 5; specific values: id 2 = 1, id 3 = 4, all others = 5.
- All four updated docs (`INTERFACE_CONTRACT.md`, `.planning/FSM_TOUCHDESIGNER_SPEC.md`, `.planning/PROJECT.md`, `data/README.md`) contain the strings asserted in acceptance_criteria.
- Existing locked-decisions and old TouchDesigner runtime contracts intact (no accidental section deletions — checked via still-present anchor strings like `Closed-loop CV` and `## 13. Minimum Prototype Contract`).
- The two firmware docs both name GPIO 34, the DollaTek module, and the new Serial line shapes.
</verification>

<success_criteria>
- ROADMAP Phase 02.1 success criteria #4 and #5 satisfied (INTERFACE_CONTRACT.md sections extended; FSM spec HEIGHT row updated; per-method max_floors honored).
- Phase 02.1 doc consistency: every reference to "ArUco dial ID-10" as the HEIGHT input either retired or routed to the new Serial floor channel.
- No regression of unrelated doc content.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-height-slider/02.1-03-SUMMARY.md` per the standard template, documenting: max_floors values committed to `methods_db.json`, the specific JSON insertion point (after `max_pucks`), section anchors edited in INTERFACE_CONTRACT.md / FSM spec / PROJECT.md, and any cleanup needed downstream (e.g. nothing reads `aruco_id10_height.png` anymore — archive note is sufficient).
</output>
