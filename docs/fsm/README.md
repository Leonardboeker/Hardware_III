# docs/fsm/

Working drafts of the finite state machine.

The canonical FSM ships from here — when you change it, regenerate the diagram in `deliverables/` so what we present matches what we build.

States as of Session 2: see top-level `README.md` "FSM States" section. Update both this folder and that section in lockstep.
